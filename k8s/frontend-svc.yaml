apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: debug-logger
    tier: frontend
spec:
  type: NodePort
  selector:
    app: debug-logger
    tier: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
