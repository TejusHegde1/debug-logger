apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: debug-logger
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: debug-logger
    tier: backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
